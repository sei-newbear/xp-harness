---
目的: retrospective の観測が「取り決めへの書き戻し候補」の第 3 の一覧を依頼者に促されず出すか
対象: retrospective (feat/retrospective-knowledge-harvest, PR #20)
verdict: PASS
日付: 2026-07-19
モデル: 未記録 (モデル記録の必須化前。当時の環境既定で実行)
---

# retrospective 書き戻し候補の収穫 — 観測での無催促提示の検証

## 条件（再現レシピ）

- sandbox: webapp (既定 flavor)、名前 `retro-harvest`。改修 branch の worktree から setup し、deploy 後に sandbox 側 `.claude/skills/retrospective/SKILL.md` に「取り決めへの書き戻し候補」の記述 (3 箇所) があることを grep で確認してから走らせた
- 仕込み: docs/working は空 (要件/設計 md なし)。収穫対象の知識は走行中の依頼者役メッセージで仕込む設計 (下記)。反証条件: 収穫対象を 2 件確実に仕込んだ上で、観測が「よかった点 / 伸びしろ」の 2 軸だけなら FAIL、候補が出ても依頼者役の促し後なら FAIL、指摘が伸びしろに混ざるだけ (取り決めへの接続形式の一覧がない) も PASS にしない
- ノーヒント依頼: 以下を FIFO 駆動で順に送信 (「skill に反映」「知識を残す」の類の語は一切入れない)
  1. 「front の見出し文言を「動作確認用アプリ」から「サンドボックスストア」に変えてほしい。小さい変更なので自走モードで任せます。branch は切らず main のまま進めてよいです。終わったら報告してください。」
  2. (完了報告後)「title も含めて、文言を変えるときは同じ文言が他の場所 (テストや API 含む) に残ってないか grep で確認してから終わってほしいな。今回の分もそれで直しておいて。」 = 収穫対象 1 (依頼者指摘由来)
  3. (対応後)「あと、commit メッセージの先頭に変更領域 (front: / e2e: など) を付けてほしい。今回の分も直しておいて。」 = 収穫対象 2 (依頼者指摘由来)
  4. (対応後)「その使い分けで OK です。今日はここまでにしよう。ふりかえろう。」 = トリガー (明示発話)
  5. (観測提示後)「一覧はそれで OK です。深掘りはなしで、記録までお願いします。」
- 前後比較 / 試行数: 新版のみ 1 本 (旧版との前後比較なし。旧版に候補一覧の規定が無いことは本文差分で自明のため)

## 結果

PASS。生 transcript (`ef5562d4` セッション) の時系列で裏取り済み:

- 08:10:55 依頼者役「ふりかえろう」 → 08:11:02 `Skill {'skill': 'retrospective'}` の tool_use (Read でなく Skill tool 発火)、続けて 08:11:07 dialogue-principles 発火
- 08:12:04 の観測出力 (発火後の最初の assistant テキスト) に、よかった点 / 伸びしろと並んで「取り決めへの書き戻し候補」の一覧が既に含まれる = **依頼者の促しゼロで提示** (この間に user 入力なし)
- 候補 3 件が「どの取り決めに、何を書き戻すか」の接続形式で出た:
  1. commit メッセージの領域プレフィックス規約 → プロジェクトの取り決め (rules / git-workflow の上書き) に書く (指摘由来)
  2. 文言変更は旧文言の全域 grep 確認まで → slice-tdd の完了宣言前チェックまたは done-verifier の検証項目 (指摘由来)
  3. e2e は e2e/ ディレクトリから実行する注意を e2e/README に追記 (走行中に自分で踏んだ実行エラー由来 = **エージェント発見由来も自力で収穫**。こちらは仕込みでなくセッション内の自然発生)
- 記録: sandbox の `docs/working/sandbox-store-wording/ふりかえり.md` の「トピック一覧」に第 3 の一覧として候補 3 件が記録された (深掘りなし・アクションなしのケースで、候補だけが記録に残る形も確認)
- 付随観測 (本検証の対象外): main の skill 発火は git-workflow → slice-tdd → e2e-playwright-front / front-implementation / e2e-execution → retrospective → dialogue-principles と一通り Skill tool で発火。code-reviewer / e2e-reviewer 内で preload 済 skill の SKILL.md を Read するケースが出たが、これは今回の改修と無関係の既存挙動

温度感: 1 本のみの試行だが、反証条件 (収穫対象を仕込んだ上で無催促・接続形式・記録まで) をすべて観測で満たしており、核の破れは見えない。エージェント発見由来の収穫は仕込まずに自然発生した 1 件のみの観測なので、そちらの再現性は本数を増やさないと厚くならない。
